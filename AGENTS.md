# Repository Agent Notes

- Preferred worktree parent directory: `../vcs-rewview-flow-worktrees`
- Create each new worktree as a child of that parent directory.
- When the user says `install locally`, run the manual installation script for both Codex and Claude Code using user-global symlink installs.
